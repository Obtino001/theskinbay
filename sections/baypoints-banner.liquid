<style>
.sb-{{ section.id }} {
  background: #F6F2EB;
  padding: 14px 20px;
  border-top: 1px solid rgba(0,0,0,0.07);
  border-bottom: 1px solid rgba(0,0,0,0.07);
}
.sb-wrap {
  max-width: 1400px;
  margin: 0 auto;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 32px;
  flex-wrap: wrap;
}
.sb-label {
  font-family: var(--font-body--family), sans-serif;
  font-size: 9px;
  font-weight: 700;
  letter-spacing: 0.18em;
  text-transform: uppercase;
  color: #111;
  white-space: nowrap;
}
.sb-divider {
  width: 1px;
  height: 24px;
  background: rgba(0,0,0,0.15);
  flex-shrink: 0;
}
.sb-flow {
  display: flex;
  align-items: center;
  gap: 6px;
}
.sb-step {
  display: flex;
  align-items: center;
  gap: 6px;
  font-family: var(--font-body--family), sans-serif;
  font-size: 9px;
  letter-spacing: 0.06em;
  color: #555;
  white-space: nowrap;
}
.sb-step strong {
  color: #111;
  font-weight: 700;
}
.sb-arrow {
  color: #C8A96A;
  font-size: 10px;
  margin: 0 2px;
}
.sb-value-inline {
  font-family: var(--font-heading--family), serif;
  font-size: 15px;
  font-weight: 600;
  color: #C8A96A;
  white-space: nowrap;
  letter-spacing: 0.02em;
}
.sb-btn {
  display: inline-block;
  padding: 8px 20px;
  background: #000;
  color: #fff;
  text-decoration: none;
  font-family: var(--font-body--family), sans-serif;
  font-size: 8px;
  font-weight: 700;
  letter-spacing: 0.14em;
  text-transform: uppercase;
  white-space: nowrap;
  transition: opacity 0.15s ease;
}
.sb-btn:hover { opacity: 0.8; }

@media (max-width: 749px) {
  .sb-wrap { gap: 14px; }
  .sb-divider { display: none; }
  .sb-label { display: none; }
}
</style>

<div class="sb-{{ section.id }}">
  <div class="sb-wrap">

    <span class="sb-label">SkinBay Rewards</span>

    <div class="sb-divider"></div>

    <div class="sb-flow">
      <div class="sb-step"><strong>Spend</strong><span>shop as usual</span></div>
      <span class="sb-arrow">›</span>
      <div class="sb-step"><strong>Earn</strong><span>£2 = 1 point</span></div>
      <span class="sb-arrow">›</span>
      <div class="sb-step"><strong>Collect</strong><span>100 points</span></div>
      <span class="sb-arrow">›</span>
      <div class="sb-step"><strong>Reward</strong><span>£5 voucher</span></div>
    </div>

    <div class="sb-divider"></div>

    <span class="sb-value-inline">£5 back every 100 points</span>

    <a href="https://theskinbay.co.uk/pages/skinbay-loyalty-rewards" class="sb-btn">View Rewards</a>

  </div>
</div>